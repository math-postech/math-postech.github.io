课程页面怎么组织？最简单的方案：一个 `readme.md` 当主页，把课程信息、作业链接、讲义链接全部放在一页里。

讲义按 topic 命名（比如 `vector-spaces.md`），不按周数命名——这样调课不用改文件名。作业 PDF 扔进 `files/` 目录，主页里挂个下载链接就行。

不搞 sidebar，不改 Docsify 配置，不拆多页。教授加新内容就往 `readme.md` 里追加一行链接，维护成本最低。

如果非要搞 sidebar 或者按周命名讲义，维护负担会随学期推进越来越重，得不偿失。
